{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/./master/nextflow_schema.json",
  "title": ". pipeline parameters",
  "description": "",
  "type": "object",
  "definitions": {
    "input_output_options": {
      "title": "Input/output options",
      "type": "object",
      "fa_icon": "fas fa-terminal",
      "description": "Define where the pipeline should find input data and save output data.",
      "required": ["outdir", "SAMPLEFILE", "reportdir"],
      "properties": {
        "SAMPLEFILE": {
          "type": "string",
          "description": "The sample file provided to the pipeline with one sample ID per line.  This sample is assumed to have CRAM files stored on IRODS."
        },
        "outdir": {
          "type": "string",
          "format": "directory-path",
          "description": "The output directory where the results will be saved. You have to use absolute paths to FARM locations (i.e. starting with /lustre or /nfs)",
          "fa_icon": "fas fa-folder-open"
        },
        "reportdir": {
          "type": "string",
          "description": "The output directory where the log files will be saved. You have to use absolute paths to FARM locations (i.e. starting with /lustre or /nfs)"
        },
        "reference": {
          "type": "string",
          "default": "/nfs/cellgeni/STAR/human/2020A/index",
          "description": "Tells pipeline which genome to use for alignment (by default GRCh38 2020A is used)."
        },
        "keep_bams": {
          "type": "string",
          "default": "false",
          "description": "Tells the pipeline whether to generate BAM files (default false means do not generate)."
        },
        "keep_fastqs": {
          "type": "string",
          "default": "false"
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "#/definitions/input_output_options"
    }
  ]
}
